import { ExceptionsInterceptor } from './exceptions.interceptor';

describe('ExceptionsInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ExceptionsInterceptor()).toBeDefined();
  });
});
